name: Lean Build

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Build Lean Project
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - uses: leanprover/lean-action@v1
